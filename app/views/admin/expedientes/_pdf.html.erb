<%= semantic_form_for @expediente.assets.build, :url => adjuntar_admin_expediente_path(@expediente) do |f| %>

  <%= f.inputs :asset %>

  <%= f.actions do %>
    <%= f.action :submit, :as => :button %>
    <%= f.action :cancel, :as => :link %>
  <% end %>

<% end %>

