<%= semantic_form_for [nota, pase] do |f| %>
    <%= f.inputs do %>
    <%= f.input( :area ) %>
    <%= f.input :ingreso, :as=>:string, :input_html => {:class => 'datepicker'} %>
    <%= f.input( :descripcion ) %>
  <% end %>
  <%= f.actions %>
<% end %>
